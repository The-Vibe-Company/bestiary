- Toujours rendre les boutons clickables
- Use the CLI command `agent-browser` for testing the localhost website.
- Use the Skill `agent-browser` to understand the browser automation CLI tool
- Always use Context7 MCP when I need library/API documentation, code generation, setup or configuration steps without me having to explicitly ask.
